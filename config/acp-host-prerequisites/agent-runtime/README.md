This placeholder makes the unprovisioned `acp-host` Compose profile fail closed.
Bind a reviewed local, network-free ACP agent root here only for local experiments;
production qualification should set `ACP_AGENT_RUNTIME_ROOT` to its absolute path.
