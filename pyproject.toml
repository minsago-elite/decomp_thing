[project]
name = "decomp-engine"
version = "0.1.0"
description = "LLM-assisted binary-to-C reconstruction tooling."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
