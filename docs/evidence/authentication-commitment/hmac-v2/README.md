This dirty-worktree run passed all 25 selected tests with no failures, errors or skips. The manifest records the base commit, tracked diff digest, exact changed-file hashes and four original JUnit hashes. It is evidence for those tested files, not a clean-checkout run.

Gradle test selectors were AcpAuthenticationInventoryTest, AcpDoctorPreflightTest, WebAuthenticationInspectionTest and only DoctorTest.`authentication previews escape terminal controls and Unicode formatting`, with offline non-incremental compilation and DECOMP_REQUIRE_LIVE_ACP_CONTRACT=1. Initialize-only preflight uses containment; no authentication RPC or model prompt is executed. Two separate fixture JVMs capture an empty inventory to verify scope/key rotation without launching an ACP agent.

Coverage includes repeated same-scope comparison, full SDK-retained payload changes, redacted previews, rejection of unkeyed digest output, web commitment/format/scope fields, and doctor descriptor migration. It does not prove cross-JVM equality, which the new scope explicitly disallows, nor resolve separate preview-redaction and inspection-generation findings or qualify current-master integration. Full CI and vulnerability-reproduction lanes were not selected.
