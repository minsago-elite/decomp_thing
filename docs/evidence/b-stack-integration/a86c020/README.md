Base integration candidate `a86c020` (draft #366, issue #360) passed 46 legacy/service/lifetime/uncertainty-helper/shutdown tests and 41 SPA/API/access/asset tests, with zero failures, errors or skips. The first group ran on the resolved merge immediately before committing identical implementation files; the second ran on the clean commit. This distinction is recorded in the manifest, which hashes eight original JUnit reports.

Run the Gradle test task with separate `--tests` selectors for the classes listed in each manifest group, offline, using `-PfrontendNodeHome` for Node 24.20.0/npm 11.19.0. The pinned frontend build and verified nine-file asset closure are included through the normal resource tasks; its generated manifest is retained here. No ACP peer, model prompt, compiler target or vulnerability-reproduction test is selected.

The initial run found that an absent root was not owned at server startup (42 passed, four fixture failures); explicit server initialization fixed this while preserving standalone read-only initialization. The passing runs cover root exclusion, handler/worker lifetime, retry cleanup, unchanged historical metadata with interrupted API projections, typed diagnostics and access gates. They do not qualify the later B stack, both production staging uncertainty paths, all recovery policies or remaining reviews. #360 remains open and this draft is not ready for independent merge.
