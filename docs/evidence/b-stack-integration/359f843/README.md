Clean source `359f843cbe7d6895cf77fbd45cbe4ac8b5469a2b` integrates #259 crash-test assertions dd6e4a8 through #332. All 18 tests in AgentSessionJournalTest and AgentSessionJournalCrashTest passed offline with zero failures, errors or skips. The manifest hashes the two original JUnit reports. Reproduce with the Gradle test task and separate `--tests` selectors for these classes.

The crash cases now require the unverified-process-cleanup decision on both reopen attempts and compare modification time captured before the first post-crash acceptance replay. Existing source-byte, cursor, turn-count and later replay assertions remain. The fixture JVMs operate on generic text journals and halt at explicit markers; no ACP peer, compiler or target runs. These checks do not establish surviving external-process cleanup, automatic recovery or exactly-once dispatch. Full CI and vulnerability-reproduction lanes were not selected.
