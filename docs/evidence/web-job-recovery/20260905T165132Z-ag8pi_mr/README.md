Clean source `e8ff154` integrates prospective recovery replacement validation from b3de78f (#321) through the shared listing/recovery scan and #332. The original manifest and 13 byte-identical JUnit reports record 104 passing tests, with no failures, errors or skips. Reproduce with `python3 scripts/verify-web-job-recovery.py`.

The new legacy-record regression exercises both candidate positions, validates the old record remains readable, and checks unchanged bytes for every job after replacement validation fails. Restoring valid records allows recovery. Existing bounded inspection, cancellation, publication failure, process-death, ownership and web handoff cases remain covered. This does not resolve the separate cancellation/write-admission handshake review, qualify current-master integration or complete B-series acceptance. Full CI and vulnerability-reproduction lanes were not selected.

Source snapshot reachability: the recorded commit `e8ff154aca462f3c684ba04543905d16d5116825` was created on the #332 review-integration lineage and is not an ancestor of this audit branch. It is retained under the lightweight tag `evidence/web-job-recovery/20260905T165132Z-ag8pi_mr` and is currently reachable from the pushed branch `b-auth-method-commitment`. Resolve it with `git fetch origin 'refs/tags/evidence/web-job-recovery/20260905T165132Z-ag8pi_mr:refs/tags/evidence/web-job-recovery/20260905T165132Z-ag8pi_mr'` (or by fetching `b-auth-method-commitment`) and verify with `git cat-file -e e8ff154aca462f3c684ba04543905d16d5116825^{commit}`. The manifest embeds the same full SHA in its `commit` field.
