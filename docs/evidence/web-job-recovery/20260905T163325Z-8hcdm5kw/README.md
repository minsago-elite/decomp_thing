Clean source `d1db587` integrates the failed-start listener deadlock fix and bounded recovery publication through the B stack, retaining early stop signaling, recovery cancellation and shared listing inspection. The original manifest and 13 byte-identical JUnit reports record 103 passing tests, with no failures, errors or skips. Reproduce with `python3 scripts/verify-web-job-recovery.py`.

This verifies the combined storage/recovery changes, including queued pre-start requests and duplicate-start rejection. It does not qualify integration with current master or close other outstanding reviews. Full CI and vulnerability-reproduction lanes were not selected.

Source snapshot reachability: the recorded commit `d1db5876aa6e64bad256be104544fd2c9a336a8e` was created on the #332 review-integration lineage and is not an ancestor of this audit branch. It is retained under the lightweight tag `evidence/web-job-recovery/20260905T163325Z-8hcdm5kw` and is currently reachable from the pushed branch `b-auth-method-commitment`. Resolve it with `git fetch origin 'refs/tags/evidence/web-job-recovery/20260905T163325Z-8hcdm5kw:refs/tags/evidence/web-job-recovery/20260905T163325Z-8hcdm5kw'` (or by fetching `b-auth-method-commitment`) and verify with `git cat-file -e d1db5876aa6e64bad256be104544fd2c9a336a8e^{commit}`. The manifest embeds the same full SHA in its `commit` field.
